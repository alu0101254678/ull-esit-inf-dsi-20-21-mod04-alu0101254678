/**
 * Funcion que suma dos numeros enviados como
 * parametros
 * @param number1 el primer operando de la suma
 * @param number2 el segundo operando de la suma
 * @returns el resultado de la suma
 */
export function add(number1: number, number2: number) {
  return number1 + number2;
}
